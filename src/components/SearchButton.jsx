function SearchButton() {
    return (
        <button
            type="button"
            className="bg-gray-700 hover:bg-cyan-600 text-white w-10 h-10 rounded flex items-center justify-center transition-colors"
            aria-label="Buscar"
        >
            🔍
        </button>
    );
}

export default SearchButton;
